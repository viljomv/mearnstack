// //map() clear
// filter() clear
// reduce() 
// sort()
// some()
// find()
// forEach()

// var arr=[2,4,5,8,10]   
// // var square=arr.map(num=>num**2)
// // console.log(square);

// var add=arr.map(num=>num+2)
// console.log(add);

// var names=["viljo,shifas,dathan,anik,adarsh"]
// var name1=names.map(name=>name.toUpperCase())
// console.log(name1);
// var name1=arr.map(nam=>nam[1])
// console.log(name1);

// var evens=arr.filter(num=>num%2==0) filter(condition check by filter)
// console.log(evens);

// var names=["arun","shifas","dathan","anik","adarsh"]
// var anames=names.filter(name=>name[1]=="d")
// console.log(anames);

var arr=[10,20,30,40,50]
var sub=arr.map(num=>num-2)
console.log(sub);

var arr=["viljo,shifas,fancy,vilme,mariya,geo"]
var name=arr.map(names=>names.toUpperCase())
console.log(name);